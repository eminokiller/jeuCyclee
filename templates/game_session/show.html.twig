{% extends 'base.html.twig' %}

{% block title %}GameSession{% endblock %}

{% block body %}
    <h1>GameSession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_session.id }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ game_session.duree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_session_index') }}">back to list</a>

    <a href="{{ path('game_session_edit', {'id': game_session.id}) }}">edit</a>

    {{ include('game_session/_delete_form.html.twig') }}
{% endblock %}
