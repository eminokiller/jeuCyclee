<form method="post" action="{{ path('action_marketing_delete', {'id': action_marketing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ action_marketing.id) }}">
    <button class="btn alert-danger" style="width: 100%">SUPPRIMER</button>
</form>
